# Changelog

## v1 — first public version
- Commit messages as a decision log; comments as intent; the provenance pointer (`source: session <id> @ <timestamp>`); read-the-log-first; pathspec commits on shared trees.
